package Players

import PlayerConfigurator
import HashList

public let playersInstances = new HashList<PlayerConfigurator>()

public function initPlayers()
	for i = 0 to bj_MAX_PLAYER_SLOTS - 1
		if players[i].isIngame()
			playersInstances.add(new PlayerConfigurator(players[i]))
